syntax = "proto3";

option java_multiple_files = true;
option java_package = "user";

service UserService{
    rpc UserValidation(ValidationRequest) returns (ValidationResponse);
}

message ValidationRequest{
    string userId = 1;
}

message ValidationResponse{
    bool isValid = 1;
}
